{
  "name": "sky-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:icons": "node scripts/fetch-apple-icons.mjs",
    "test": "node --test",
    "dev": "npm run prepare:icons && vite",
    "build": "npm run prepare:icons && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest"
  },
  "devDependencies": {}
}
