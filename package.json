{
  "name": "sky-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:icons": "node scripts/fetch-apple-icons.mjs",
    "prepare:sky": "node scripts/prepare-sky-assets.mjs",
    "test": "node --test",
    "dev": "npm run prepare:icons && npm run prepare:sky && vite",
    "build": "npm test && npm run prepare:icons && npm run prepare:sky && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest"
  },
  "devDependencies": {}
}
